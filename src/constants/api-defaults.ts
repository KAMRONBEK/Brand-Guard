/** Default backend origin when env vars omit overrides. */
export const DEFAULT_API_ORIGIN = "https://comment-api.fapi.uz";

/** Default `language` hint for comment-api requests (workflow forms). */
export const DEFAULT_COMMENT_API_LANGUAGE_HINT = "ru";
